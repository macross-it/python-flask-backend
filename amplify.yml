version: 1
frontend:
  phases:
    preBuild:
      commands:
        - docker build -t flask-backend .
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
