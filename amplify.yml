version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - docker build -t python-backend .
        build:
          commands:
            - echo "Build done"
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
